export default function Placeholder({ title }) {
  return (
    <section className="placeholder-page">
      <div className="placeholder-content">
        <span className="placeholder-eyebrow">School ERP</span>

        <h1>{title}</h1>

        <p>This page is connected to the application routing and layout.</p>
      </div>
    </section>
  );
}
